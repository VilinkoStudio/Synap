name: Android

on:
  push:
    branches:
      - master
    tags:
      - 'android-*'
    paths:
      - 'android/**'
      - 'core/**'
      - 'coreffi/**'
      - 'xtask/**'
      - 'Cargo.toml'
      - 'Cargo.lock'
      - '.github/workflows/android.yml'
  pull_request:
    branches:
      - master
    paths:
      - 'android/**'
      - 'core/**'
      - 'coreffi/**'
      - 'xtask/**'
      - 'Cargo.toml'
      - 'Cargo.lock'
      - '.github/workflows/android.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: android-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always
  ANDROID_COMPILE_SDK: '36'
  ANDROID_BUILD_TOOLS: '35.0.1'
  ANDROID_NDK_VERSION: '28.0.13004108'

jobs:
  build:
    name: Build Android
    runs-on: ubuntu-latest
    timeout-minutes: 60
    env:
      ANDROID_KEYSTORE_BASE64: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
      ANDROID_KEYSTORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
      ANDROID_KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
      ANDROID_KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Gradle cache
        uses: gradle/actions/setup-gradle@v4

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android SDK components
        run: |
          yes | sdkmanager --licenses > /dev/null
          sdkmanager \
            "platform-tools" \
            "platforms;android-${ANDROID_COMPILE_SDK}" \
            "build-tools;${ANDROID_BUILD_TOOLS}" \
            "ndk;${ANDROID_NDK_VERSION}"

      - name: Export Android NDK toolchain environment
        run: |
          set -euo pipefail

          ANDROID_NDK_ROOT="${ANDROID_SDK_ROOT}/ndk/${ANDROID_NDK_VERSION}"
          TOOLCHAIN_BIN="${ANDROID_NDK_ROOT}/toolchains/llvm/prebuilt/linux-x86_64/bin"
          ANDROID_API_LEVEL=24

          {
            echo "ANDROID_NDK_ROOT=${ANDROID_NDK_ROOT}"
            echo "ANDROID_NDK_HOME=${ANDROID_NDK_ROOT}"
            echo "PATH=${TOOLCHAIN_BIN}:${PATH}"
            echo "CC_aarch64_linux_android=${TOOLCHAIN_BIN}/aarch64-linux-android${ANDROID_API_LEVEL}-clang"
            echo "CC_armv7_linux_androideabi=${TOOLCHAIN_BIN}/armv7a-linux-androideabi${ANDROID_API_LEVEL}-clang"
            echo "CC_i686_linux_android=${TOOLCHAIN_BIN}/i686-linux-android${ANDROID_API_LEVEL}-clang"
            echo "CC_x86_64_linux_android=${TOOLCHAIN_BIN}/x86_64-linux-android${ANDROID_API_LEVEL}-clang"
            echo "CARGO_TARGET_AARCH64_LINUX_ANDROID_LINKER=${TOOLCHAIN_BIN}/aarch64-linux-android${ANDROID_API_LEVEL}-clang"
            echo "CARGO_TARGET_ARMV7_LINUX_ANDROIDEABI_LINKER=${TOOLCHAIN_BIN}/armv7a-linux-androideabi${ANDROID_API_LEVEL}-clang"
            echo "CARGO_TARGET_I686_LINUX_ANDROID_LINKER=${TOOLCHAIN_BIN}/i686-linux-android${ANDROID_API_LEVEL}-clang"
            echo "CARGO_TARGET_X86_64_LINUX_ANDROID_LINKER=${TOOLCHAIN_BIN}/x86_64-linux-android${ANDROID_API_LEVEL}-clang"
          } >> "$GITHUB_ENV"

      - name: Set up Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-linux-android,armv7-linux-androideabi,i686-linux-android,x86_64-linux-android

      - name: Cache Rust build outputs
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: |
            . -> target

      - name: Prepare Android signing config
        if: startsWith(github.ref, 'refs/tags/') && env.ANDROID_KEYSTORE_BASE64 != '' && env.ANDROID_KEYSTORE_PASSWORD != '' && env.ANDROID_KEY_ALIAS != '' && env.ANDROID_KEY_PASSWORD != ''
        run: |
          echo "${ANDROID_KEYSTORE_BASE64}" | base64 --decode > android/upload-keystore.jks

      - name: Build Android app
        working-directory: android
        env:
          SIGNING_STORE_FILE: ${{ github.workspace }}/android/upload-keystore.jks
          SIGNING_STORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
          SIGNING_KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
          SIGNING_KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}
        run: |
          chmod +x gradlew
          if [[ "${GITHUB_REF}" == refs/tags/* ]]; then
            ./gradlew --no-daemon assembleRelease
          else
            ./gradlew --no-daemon assembleDebug
          fi

      - name: Collect release assets
        if: startsWith(github.ref, 'refs/tags/')
        run: |
          set -euo pipefail
          shopt -s nullglob

          mkdir -p dist

          apk_files=(android/app/build/outputs/apk/release/*.apk)
          if [[ ${#apk_files[@]} -eq 0 ]]; then
            echo "No release APK files were produced."
            exit 1
          fi

          for apk in "${apk_files[@]}"; do
            cp "$apk" "dist/synap-android-${GITHUB_REF_NAME}-$(basename "$apk")"
          done

          symbols="android/app/build/outputs/native-debug-symbols/release/native-debug-symbols.zip"
          if [[ -f "$symbols" ]]; then
            cp "$symbols" "dist/synap-android-${GITHUB_REF_NAME}-native-debug-symbols.zip"
          fi

          metadata="android/app/build/outputs/apk/release/output-metadata.json"
          if [[ -f "$metadata" ]]; then
            cp "$metadata" "dist/synap-android-${GITHUB_REF_NAME}-output-metadata.json"
          fi

      - name: Upload release artifact
        if: startsWith(github.ref, 'refs/tags/')
        uses: actions/upload-artifact@v4
        with:
          name: synap-android-release
          path: dist/*
          if-no-files-found: error

  release:
    name: Publish Release
    if: startsWith(github.ref, 'refs/tags/')
    needs:
      - build
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Download release artifacts
        uses: actions/download-artifact@v4
        with:
          name: synap-android-release
          path: release-assets

      - name: Publish GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: release-assets/*
          generate_release_notes: true
