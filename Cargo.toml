[workspace]
members = [
    "core",
    "corenet",
    "coreffi-shared",
    "coreffi",
    "coreffi-uniffi029",
    "cli",
    "xtask",
    "desktop_linux",
    "relay",
]
resolver = "2"

[profile.wasm-release]
inherits = "release"
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
