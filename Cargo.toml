[workspace]
members = [
    "core",
    "coreffi",
    "cli",
    "desktop",
    "tools/importers/xiaomi-notes",
    "xtask",
    "web",
]
resolver = "2"

[profile.wasm-release]
inherits = "release"
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
