[workspace]
members = [
    "core",
    "corenet",
    "coreffi",
    "cli",
    "xtask",
    "web", "desktop_linux", "relay",
]
resolver = "2"

[profile.wasm-release]
inherits = "release"
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
